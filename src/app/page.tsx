import React from 'react';
import Image from 'next/image';
import Home from '@/components/Home';

const page = () => {
    return (
        <main>
            <Home/>
        </main>
    );
};

export default page;

